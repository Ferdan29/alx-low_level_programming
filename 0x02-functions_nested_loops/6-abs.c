#include "main.h"

/**
 * _abs - function that print the absolute value of an integer
 *@n: The character to be checked.
 * Return:0 and 1
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
