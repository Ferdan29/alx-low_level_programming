#include "main.h"

/**
 * add - function to add to numbers
 * @re: the character to check
 *
 * Return: the result
 */
int add(int k, int y)
{
	int re;

	re = k + y;

	_putchar(re);

	return (re);
}

