#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length
 * of a string.
 * @*s: pointer variable
 * s: variable
 *
 * Return: Always 0.
 */

size_t _strlen(const char *str)
{
	int d;

	d = strlen(*str);
}
